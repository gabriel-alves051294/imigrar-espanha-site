
# PocketBase Setup

OAuth providers (Google, Facebook, etc.) must be configured manually via PocketBase Admin UI → Settings → Auth providers. Do NOT attempt to set OAuth credentials via migration files. Configure providers directly in the admin panel and test before deploying to production.
